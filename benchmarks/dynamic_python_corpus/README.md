# Dynamic Python qualification corpus

This retained corpus separates one statically resolvable control call from registry,
reflection, runtime import, and monkey-patch boundaries. The scanner must resolve the
control and preserve every dynamic call site without inventing a unique static target.
It is a regression fixture, not independent real-world qualification evidence.
