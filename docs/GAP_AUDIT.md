# SFMEA guidance coverage audit

Last implementation audit: 2026-08-04

This matrix compares PySFMEA with the public NASA and FAA process guidance used by the project. “Implemented” means the repository contains a tested capability; it does not mean that scanner output has been accepted by a qualified engineering team.

| Guidance expectation | Current implementation | Evidence |
|---|---|---|
| Define the analyzed system, mission, boundary, modes/states, must-work/prohibited functions, safe/degraded states, humans, timing/resources, deployment, criticality, assumptions, exclusions, phase, and ground rules | Implemented through `sfmea.toml` and a hashed resolved context manifest; read-only preflight and post-scan gates preserve incomplete inputs as limitations and unresolved questions instead of silently filling them | `system_context.py`, `sfmea doctor`, context/coverage report view, `system-context.json` package artifact |
| Construct a functional/block or data-flow view | Implemented as conservative architecture, static/observed sequence, and collision-safe requirement-to-hazard trace graphs in Mermaid or JSON, plus a validated canonical diagram model for generated/custom inline-SVG architecture, interface, propagation, control, traceability, state, flow, and sequence views | `architecture.py`, `visuals.py`, `diagrams.py`, `html_report.py`, diagram/report CLI commands |
| Combine bottom-up SFMEA with top-down Software Fault Tree Analysis | Explicit hazard trees support validated typed events and AND/OR/VOTE/INHIBIT gates, cycle/reference checking, renderer-neutral diagrams, placeholders for missing decomposition, and bidirectional event/finding reconciliation gaps without inferring causal sufficiency | `sfta.py`, `config.py`, `validation.py`, `diagrams.py`, HTML SFTA explorer, `sfmea sfta` |
| Categorize elements and account for unanalyzed regions | Functions, methods, private/nested callables, constructors, lifecycle methods, lambdas, executable module initialization, declarative data models, dependency environment, common causes, framework entrypoints, and local interface contracts are inventoried. A separate bounded inventory classifies every visited artifact/region as analyzed, indexed, excluded, unresolved, or opaque | `scanner.py`, `repository_inventory.py`, context/coverage report, `repository-inventory.json` |
| Identify functional and interface failure modes | Baseline functional guidewords plus internal/external interface, data, timing, calculation, logic, state, environment, hardware, resource, detection, and project-specific rules | `scanner.py` candidate rules |
| Consider bad data and event abnormalities | Input/model/serialization rules cover missing, wrong, stale, duplicate, range, precision, compatibility, omission, order, and timing prompts | scanner rules and methodology mapping |
| Identify specific potential causes | Each generated item contains rule-specific cause prompts; reviewers can replace or extend them; an accepted item cannot omit causes by default | analysis schema, validation, browser reviewer |
| Trace local, next-higher, and end effects | Three separate fields, transitive upstream paths, hazard linkage, and default completeness gates | `validation.py`, `architecture.py`, reviewer |
| Identify detection methods and compensating provisions | Separate existing prevention and detection controls, evidence, recommended actions, and implemented actions; action-required records cannot omit the action description/owner/date by default. Circuit breakers are first-class static candidates with extracted roles, state/timing/isolation/fallback models, dedicated failure modes and diagrams, and controlled-clock/concurrency/fault-injection obligations; extraction never credits control effectiveness | model, scanner resilience-control adapter, assurance planner, validation, reviewer, diagrams, exports |
| Identify safety requirements and hazards | Governed requirement/hazard catalogs and component mappings; unknown, unmatched, and orphaned references are reported | `config.py`, `validation.py` |
| Evaluate worst credible consequences | Ground rules and severity rationale are required by default; configured hazard severity may seed but never approve a record | configuration and quality gates |
| Support severity-focused SFMEA and defined organizational scales | Numeric severity, categorical severity, optional numeric S/O/D, and RPN arithmetic are supported | model, configuration, validation |
| Identify corrective actions and assess residual risk | Owner/date, actions taken, verification evidence, post-action ratings/category, and closure gates | model and validation |
| Assess impact of corrective/design changes | Callable, module/class, SFMEA-context, parsed dependency/lockfile, and repository baselines; move/rename continuity; transitive caller impact; explicit revalidation | scanner/store rescan tests |
| Consider dependent/common-mode failures | Project-defined common causes create cross-component items and graph edges; insufficient matches are reported | config, scanner, architecture |
| Maintain the analysis throughout the lifecycle | Stable IDs, generator/schema provenance, schema migrations, scan history, field-level review history, audit CSV, baseline IDs, malformed-record rejection, strong ETag/`If-Match` stale-write refusal across browser sessions and external edits, pre-commit disk revision checks, failed-save rollback, loopback Host enforcement, unsaved-edit protection, spreadsheet-safe CSV text, checksum-manifested portable directory/ZIP review packages, atomic publication, independent path- and archive-safe verification, and optional detached Ed25519 authenticity | store, server, report, signing, `sfmea verify-package` |
| Use cross-functional review | Named review team and required decision attribution; missing/unidentified reviewers and approvers are errors, limited role diversity is reported | config, reviewer, validation |
| Document the worksheet and unresolved issues | JSON source record, CSV/Markdown worksheet, self-contained interactive HTML report, locally rendered paginated PDF, inventory, architecture, sequence, traceability, coverage, audit export, deterministic/model summaries, and machine-readable findings. HTML embeds a canonical analysis-state declaration and report-data digest for workflow staleness/tamper detection. Large HTML-only assurance and SFTA workspaces use explicit bounded projections with truthful total counts and links to the complete package registers | CLI, `html_report.py`, `workflow.py`, `pdf_report.py`, and reports |
| Trace authoritative guidance to findings | Versioned NASA/FAA/NIST/IEC/CWE source catalog; explicit applicability profiles; exact section/page locators; canonical source-record and captured-PDF hashes; typed rule mappings; per-finding citations; JSON/CSV/package exports; validation; HTML navigation; and a bounded source-to-finding diagram. Current FAA commercial-space, FAA airworthiness, NASA, security, and legacy contexts cannot leak into a generic scan without profile selection | `guidance.py`, `config.py`, `scanner.py`, `validation.py`, `report.py`, `html_report.py`, `diagrams.py`, `sfmea citations` |
| Map every failure mode to an explicit verification obligation | Every active finding receives a stable assurance contract with stimuli, operational context, safe/degraded/recovery expectations, oracles, acceptance criteria, evidence requirements, guarded lifecycle review, package exports, and report navigation. Workflow progress distinguishes accepted/applicable findings, planning readiness, implementation, execution, and independently reviewed verification. The local browser exposes accepted-finding planning with named attribution, stale-write refusal, and read-only evidence/approval states. Pytest scaffolds default to accepted findings and pending implementations, publish atomically, bind both exact governed-analysis state and a minimal selected-contract snapshot, fail closed on manifest drift, and distinguish unrelated analysis changes from materially stale test contracts without treating expected test edits as corruption. Verification replays the persisted scope/disposition/limit/implementation selection and emits obligation-level added/removed/changed diffs. Integrity-protected queue ID/owner/purpose metadata gives subsystem/team queues stable organizational identity. Guarded refresh preserves that identity and selection, accepts only intact scaffolds with untouched generated files, publishes through a temporary backup, and restores the previous queue on publication failure. `sfmea status` recommends refresh only when those preservation checks pass; edited or invalid queues remain inspection/new-destination workflows. A selection that now matches no pending obligations is explicitly reported as a retirement candidate. Guarded archival refuses active or edited queues, writes a digest-protected record of the queue, manifest, current analysis binding, and removal diff, then atomically moves the complete directory out of active status paths without deleting evidence. Status discovers conventional paths or verifies a deduplicated set of explicit queues, provides per-queue actions, and calculates current-queue portfolio coverage, assignment overlap with owner/path attribution, duplicate queue IDs, unowned queues, and uncovered accepted obligations while keeping queue organization explicitly outside the mandatory handoff gate | `assurance.py`, `server.py`, `workflow.py`, `store.py`, `validation.py`, `report.py`, `html_report.py`, `sfmea status`, `sfmea assurance`, `sfmea assurance-scaffold`, `sfmea assurance-scaffold-refresh`, `sfmea assurance-scaffold-archive`, `sfmea assurance-scaffold-verify` |
| Keep generated tests separate from evidence and independently adjudicate results | Test implementations are content-hash bound; an explicitly approved, locked-down Docker/Podman runner records as-run evidence; existing CI evidence has a bounded idempotent import; execution manifests and artifacts are re-hashed; verification requires a different reviewer, observed stimulus, every original criterion passing, and a current baseline | `execution.py`, `validation.py`, HTML evidence workspace, evidence catalog, `sfmea assurance-test-register`, `sfmea assurance-run`, `sfmea assurance-evidence-import`, `sfmea assurance-evidence-review` |
| Use automated discovery without transferring engineering authority | Grounded provider-neutral suggestions are evidence-cited, provenance-bound, schema constrained, separately reviewed, and materialized only as unreviewed records | `discovery.py`, browser and CLI suggestion workflow |
| Incorporate execution evidence | Idempotent simple and OTLP JSON imports produce baseline-linked observed relations, mapping diagnostics, and explicit incompleteness notices | `runtime.py`, sequence export, validation |
| Analyze machine-readable interface contracts | OpenAPI, Swagger, JSON Schema, and protobuf files become hashed, project-mappable contract components with extracted operations/data types, trace links, and compatibility failure prompts | `scanner.py`, inventory export, extension tests |
| Evaluate automation regression | A checked-in synthetic repository and exhaustive source-aware golden baseline require 1.0 recall/precision, stable repeated-scan digests, and guidance-profile isolation; missing/unexpected candidates are explicit and ambiguous component names are rejected rather than merged | `benchmarks/python_sfmea_corpus`, `sfmea evaluate`, `test_validation_benchmark.py` |
| Record reproducible tool and adapter provenance | An immutable scan manifest binds source/configuration/guidance selection, repository/dependency/contract inventories, environment, command safety, prompt versions, and VCS state; the typed registry publishes versioned capabilities, I/O schemas, trust, isolation, determinism, lifecycle, and run-time health. A hashed execution ledger records each adapter's status, input/output digest, and exact contributed entity IDs; findings retain many-to-one adapter provenance | `manifest.py`, `adapters.py`, run-manifest report view, `adapter-runs.json` |
| Apply licensed or organizational guidance without bundling controlled text | Strict local JSON packs contribute an automatically active `org.*` profile, controlled source metadata, revision/scope/access/quote policy, exact locators, typed rule mappings, per-record hashes, and whole-pack provenance. ID collisions and compliance claims are rejected | `guidance.py`, `docs/GUIDANCE_PACKS.md`, configuration and scanner tests |
| Exchange findings and component inventory with external tools | SARIF 2.1.0 preserves candidate semantics, stable fingerprints, relative locations, baseline and citation properties; CycloneDX 1.6 records declared dependencies and hashed manifests while explicitly identifying unresolved versions | `interchange.py`, review package, `sfmea sarif`, `sfmea sbom` |
| Compare repository analysis revisions | Canonical run differencing reports new/removed findings, causes/effects/severity/control/trace/citation changes, assumptions, dependency/configuration changes, and invalidated sufficient evidence | `interchange.py`, `sfmea diff`, interchange tests |

## Boundaries that automation cannot close

- Static analysis cannot completely resolve reflection, monkey-patching, dependency injection, native extensions, runtime service wiring, generated code, or environment-dependent dispatch. Explicit component/interface mappings and runtime evidence remain necessary.
- Source code cannot determine a credible system consequence, hazard severity, independence claim, or risk acceptance without architecture, operational knowledge, and authorized reviewers.
- SFTA correlation does not prove top-down completeness or logical sufficiency. Projects must review and approve their explicit fault-tree logic, analyze non-software contributors, and separately perform any required FTA, STPA, or other hazard analysis.
- Textual test references and line/branch coverage do not prove control effectiveness. Off-nominal tests, fault injection, mutation testing, monitoring evidence, and independent verification must be supplied where required.
- PySFMEA cannot supply a licensed proprietary Action Priority table, domain certification, tool qualification, independence, an identity provider, or a legally controlled approval signature. Optional detached package signatures authenticate bytes to a trusted key; they do not provide authorization or approval workflow.
- Occurrence is not inferred from code metrics. If used, its project meaning and evidence must be defined by the responsible organization.

## Audit sources

1. [NASA Software Engineering Handbook — Software Failure Modes and Effects Analysis](https://swehb.nasa.gov/spaces/SWEHBVD/pages/102695706/8.05+-+SW+Failure+Modes+and+Effects+Analysis)
2. [FAA Guide to Reusable Launch and Reentry Vehicle Software and Computing System Safety, Appendix B](https://www.faa.gov/sites/faa.gov/files/regulations_policies/faa_regulations/commercial_space/Guide-Software-Comp-Sys-Safety-RLV-Reentry.pdf)
3. [NASA-STD-8739.8B Software Assurance and Software Safety Standard](https://standards.nasa.gov/standard/nasa/nasa-std-87398)
4. [NASA Software Safety Guidebook, NASA-GB-8719.13](https://standards.nasa.gov/sites/default/files/standards/NASA/Baseline/0/nasa-gb-871913.pdf)
5. [FAA AC 450.141-1A, Computing System Safety](https://www.faa.gov/regulations_policies/advisory_circulars/index.cfm/go/document.information/documentNumber/450.141-1A)
6. [FAA AC 20-115D, Airborne Software Development Assurance](https://www.faa.gov/airports/resources/advisory_circulars/index.cfm/go/document.information/documentNumber/20-115D)
7. [IEC 60812:2018 publication page](https://webstore.iec.ch/en/publication/26359) — referenced but not reproduced.
