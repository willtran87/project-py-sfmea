# SFMEA guidance coverage audit

The machine-readable enhancement workbench format 7 accounts for the complete 76-item
real-repository hardening audit, the 82-item post-hardening audit, the 102-item real-run
resolution audit, and the E001-E095 product-outcome backlog. Each item has a stable
identity, product resolution, measurable acceptance criterion, live product/evidence/human state,
and an exact projection into freshness,
precision, evidence, architecture, interfaces, guidance, reporting, or qualification. Project
evidence and named decisions remain open gates by design and are not product-implementation gaps.
Format 6 also prevents a projection from masquerading as an implementation; format 7 adds
state-bound human-adjudicated canonical finding groups without deleting or implicitly disposing
their members. Every E001-E095 item
has an exhaustive, disjoint product-maturity decision, concrete source/test evidence where code
exists, a disclosed limitation, and a next action. Standalone verification rejects a rehashed
artifact that changes those curated semantics. No item is marked independently validated solely
from the product's own regression suite.

Format 4 also makes the artifact independently verifiable, separates freshness from completeness
and sufficiency, emits review-only evidence-scope remediation, plans bounded calibration samples,
and records metric provenance and report-scale limits. These projections operationalize every
item without claiming that representative evidence or an authorized decision already exists.
The real-run register is backed by operational review, onboarding, precision, architecture,
interface, temporal, guidance, performance, delivery, LLM, and qualification projections rather
than a checklist-only status claim.

2026-08-08 refinement: bounded inter-module router-table/loop composition, cross-file client base
constants and conventional request wrappers, deployed-versus-test web boundary separation,
path-level HTTP-method reconciliation, reviewer-disposition calibration, review-only architecture
mapping proposals, repository-relative cache outputs, and an embedded HTML diagnostic scorecard.
Dynamic wiring and every proposed mapping remain explicit review gaps.

2026-08-21 refinement: function, class-construction, and module-initialization analysis now prunes
only safely decidable literal/comparison/boolean/conditional/`TYPE_CHECKING` alternatives, empty
literal iteration, direct exits, selected terminal blocks, and `if/else` alternatives that all
terminate before composing calls, exceptions, sequences, and failure-mode candidates. Literal,
singleton, OR, sequence, exact built-in-dictionary mapping, capture, and wildcard `match` cases
plus static guards use the same safe selection model, including nested patterns, required-key
checks, `**rest`, and exhaustive terminal matches. Every prune is retained as validated
component-linked evidence with omission counts. Constant-true loops exclude impossible `else`
clauses and become terminal only without a break owned by that loop. Bounded `try` analysis removes
an `else` when its body cannot fall through and proves termination only for a terminal `finally` or
when every normal and handler path exits. Conditional declarations, class patterns, dynamic
mapping keys, user-defined mappings, dynamic value patterns, dynamic loop feasibility, `except*`
and complex exception/path feasibility, and general
symbolic execution remain explicit boundaries rather than inferred behavior.

The schema-backed enhancement workbench now accounts for the complete 56-item follow-on backlog,
including evidence recipes, root-cause clusters, verification portfolios, disposition queues, and
bounded system-surface candidates. Project evidence and named authority remain open by design.

Last implementation audit: 2026-08-05 · PySFMEA 0.59.0 · analysis schema 0.6

This matrix compares PySFMEA with the public NASA and FAA process guidance used by the project. “Implemented” means the repository contains a tested capability; it does not mean that scanner output has been accepted by a qualified engineering team.

| Guidance expectation | Current implementation | Evidence |
|---|---|---|
| Define the analyzed system, mission, boundary, modes/states, must-work/prohibited functions, safe/degraded states, humans, timing/resources, deployment, criticality, assumptions, exclusions, phase, and ground rules | Implemented through `sfmea.toml` and a hashed resolved context manifest; configuration uses a 5 MB regular-file/link-safe UTF-8 TOML boundary, preserves final-link identity for downstream evidence paths, and is generated through atomic prior-preserving publication. Read-only preflight and post-scan gates preserve incomplete inputs as limitations and unresolved questions instead of silently filling them | `config.py`, `system_context.py`, `sfmea doctor`, context/coverage report view, `system-context.json` package artifact |
| Relate code to deployment, shared-fate boundaries, and nested architecture | Implemented as bounded, provenance-bearing declared topology, automatic multi-component shared-fate candidates, and deterministic subsystem/source-package hierarchy with upward-only trace inheritance. Component placement remains heuristic and runtime deployment, correlated-failure probability, independence, and architecture approval remain explicit non-claims | `repository_inventory.py`, `scanner.py`, `validation.py`, Architecture report view, canonical architecture diagrams |
| Construct a functional/block or data-flow view | Implemented as conservative architecture, static/observed sequence, and collision-safe requirement-to-hazard trace graphs in Mermaid or JSON, plus a validated canonical diagram model for generated/custom inline-SVG architecture, interface, propagation, control, traceability, state, flow, and sequence views. Sequence evidence preserves repeated call sites, Python nested-call evaluation order, lexical control context, await state, lightweight annotation/constructor/import resolution provenance, and ambiguity confidence; relation-level reconciliation labels static evidence as corroborated/not-observed and runtime evidence as statically-predicted/runtime-only while retaining timing state and duration. Interface views add confidence-labeled unresolved external-call candidates. Generated JSON bundles are analysis-state bound, internally digest-protected, verified on re-import, downgrade-protected for current generator versions, atomically published, independently verifiable with versioned human or always-structured JSON verdicts, and described by offline content-addressed JSON Schema contracts. Lexical context is not path-sensitive control flow, and lightweight receiver evidence is not whole-program type inference | `architecture.py`, `visuals.py`, `diagrams.py`, `schemas.py`, `integrity.py`, `html_report.py`, `sfmea diagram`, `sfmea diagram-verify`, `sfmea schema` |
| Trace values across internal call boundaries | Implemented as a bounded path-insensitive model that binds positional and keyword caller expressions to callee parameter contracts; inventories name, attribute, and container-item sources; links callee return/yield expressions to caller assignment, argument, attribute, container, return, or discarded contexts; records ambiguous/unresolved sites and all truncation limits; indexes edges from both components; and renders a canonical `data_flow` diagram. It does not claim whole-program alias/taint soundness, runtime reachability, path feasibility, or dynamic-dispatch completeness | `scanner.py`, `scan_cache.py`, `diagrams.py`; scanner/cache/diagram tests |
| Model concurrency relationships | Implemented as a bounded static model of task spawn, join/wait, cancellation/timeout, synchronization, and awaited-completion operations. Exact component-bound records are related through lexical order, awaited-completion-before-next-operation, and conservative spawn-to-later-join candidates; totals, omissions, categories, relation kinds, and component indexes reconcile under validation. It does not prove runtime task identity, scheduler behavior, lock ownership, deadlock/race freedom, or a complete path-sensitive happens-before graph | `scanner.py`, `validation.py`, enhancement fidelity projection; scanner/enhancement tests |
| Trace typed exception propagation | Implemented as bounded explicit-raise, lexical-handler, and terminal-finalizer records plus a fixed-point projection across resolved internal calls. Selection respects nearest-`try` and first-handler order; resolves exact types, built-in inheritance, the `Exception`/`BaseException` boundary, and statically declared project inheritance; excludes nested callable bodies and statements after direct terminal exits; and retains indeterminate dynamic/ambiguous matches. Bounded branch outcomes preserve sequential reachability and merge `if`, `match`, loop, `with`, and nested-`try` alternatives for both handlers and finalizers, separating conditional/uniform rethrow, translation, new raise, fallthrough, and control exit. Bare `raise` and an explicit raise of the active catch binding preserve the original; other explicit exceptions propagate independently. A uniform bare/literal `return`, `break`, or `continue` in `finally` suppresses propagation, a uniform explicit `raise` replaces it, and an outer terminal finalizer wins over an inner one. Complete bounded finalizer outcomes, certainty, terminal basis, match provenance, exact call sites, component/finding exposure, shared cross-reference records, totals, and omissions are semantically validated. It does not prove runtime reachability, predicate feasibility, implicit exceptions, evaluated-return or non-uniform `finally` effects, dynamic/third-party alias inheritance, `ExceptionGroup` splitting, callback/native behavior, or general path feasibility | `scanner.py`, `scan_cache.py`, `validation.py`, `cross_reference.py`, enhancement fidelity projection; scanner/cache/cross-reference/enhancement tests |
| Compose resilience semantics | Implemented as one bounded interoperable model of lexical transaction lifecycle and consistency risks, fixed-point side-effect/idempotency summaries, compatible literal timing-budget constraints, nested retry amplification, class-scoped breaker semantic completeness, and explicit versus unresolved resource growth. Stable operations and component indexes reconcile exactly and semantic validation rejects dangling paths, rewritten relationships, and inconsistent totals. Static candidates do not prove runtime atomicity, exactly-once behavior, latency, breaker effectiveness, or symbolic resource complexity | `scanner.py`, `validation.py`, enhancement fidelity projection; scanner/enhancement tests |
| Federate software assurance across repository and service boundaries | A separate content-addressed assurance program binds up to 100 exact governed analyses and their baselines, validates component-to-component relationships, deadline/timeout/retry/backoff/concurrency/ordering/clock and circuit-breaker opening/recovery contracts, credits only completed digest-verified evidence, blocks failed results, uses repository-qualified finding/hazard identities, traces provider-neutral requirement snapshots and bounded external evidence to known subjects, aggregates independently produced/reviewed external validation and model-quality cohorts, requires every named role to approve the exact program through a distinct reviewer identity, and emits schema-backed JSON plus self-contained HTML/Markdown verdicts with a bounded topology view. External evidence distinguishes verified, credited, and exact-duplicate semantic claims so repeated IDs cannot inflate relationship support. Governance accounting separates declared, fully validated, and credited program approvals; malformed or pre-creation records stay visible but cannot satisfy bounded case-normalized role identifiers or readiness, and multiple valid decisions for one normalized program role become an exact uncredited conflict. Program HTML embeds the exact machine verdict and payload/program/document digests; a bounded verifier supports standalone consistency checks or exact regeneration against the retained program. Its closed verdict contract derives checks from exact error namespaces and reconciles binding/evidence totals, validation credit, LLM claims, and relationship state against linked observations and relationship-scoped findings: supported timing/recovery must be measured within deadline, timing violations must show an overrun, and passing relationship checks cannot mask invalid endpoints or violations. Minimal input rejections cannot carry unchecked summaries. Direct render/export paths enforce that contract before destination inspection, while private JSON/Markdown/HTML stages receive semantic, exact-byte, or report verification before destination-state-checked atomic replacement. It does not merge analyses, infer cross-service causality, authenticate identities, or claim certification | `program.py`, `sfmea program-init`, `sfmea program-seal`, `sfmea program-verify`, `sfmea program-report-verify`, public program/verdict/report schemas, program and package-compatibility tests |
| Combine bottom-up SFMEA with top-down Software Fault Tree Analysis | Explicit hazard trees support validated typed events and AND/OR/VOTE/INHIBIT gates, cycle/reference checking, exact-ID-or-conjunctive-glob correlation semantics, indexed ID-only matching, renderer-neutral diagrams, placeholders for missing decomposition, and bidirectional event/finding reconciliation gaps without inferring causal sufficiency. Exact definition-digest approval through governed SFTA authoring enables bounded qualitative minimal cut-set expansion with superset absorption, undeveloped/external/conditioning flags, and fail-closed logic/count/width/operation limits; it never infers probability or independence. The complete gap register remains machine-readable while validation collapses repetitive bottom-up gaps to a count and bounded sample. Package verification regenerates the complete SFTA model, flat gap register, and SFTA-dependent validation findings using producer-version-aware selector semantics, checks exact projections and count consistency, and rejects rewritten top-down analysis even when manifest hashes are recomputed | `sfta.py`, `sfta_authoring.py`, `config.py`, `validation.py`, `diagrams.py`, `report.py`, HTML SFTA explorer, `sfmea sfta`, `sfmea verify-package` |
| Categorize elements and account for unanalyzed regions | Functions, methods, private/nested callables, constructors, lifecycle methods, lambdas, executable module initialization, declarative data models, dependency environment, common causes, framework entrypoints, and local interface contracts are inventoried. Python source and textual test evidence use exact-byte identity-stable PEP 263-aware 20 MB snapshots with explicit source-file and test-index aggregate ceilings. Each selected source is read once for parsing, included-test indexing, baseline hashing, and repository-inventory evidence. Eligible test evidence honors default/hidden exclusions; explicit evidence-only globs can cross configured semantic exclusions without creating components. AST-grounded imports/calls provide attribution and bounded test-structure dimensions. Accepted/rejected source/test counts, bytes, and canonical snapshot-set digests are run-manifest bound. Dependency manifests use exact-byte identity-stable regular-file snapshots with link/containment checks and 20 MB per-file/1,000-attempted-file/100 MB aggregate limits across recursive includes. Accepted byte counts and SHA-256 values are explicit, hashing/parsing/repository coverage/baselines/manifests reuse them, and unsupported lockfile syntax remains content-addressed rather than speculatively parsed. Interface contracts use one exact-byte identity-stable regular-file snapshot with 20 MB per-file/1,000-file/100 MB aggregate bounds; JSON additionally rejects duplicate keys and non-finite values under 100-level/1,000,000-node limits before typed extraction, while operation/data-type projections stop at 500 values and malformed artifacts remain explicit inventory records. Accepted bytes drive extraction and repository coverage, while counts and digests are retained in the run-manifest-bound contract inventory. The separate inventory reuses accepted analysis/test/dependency/contract/in-repository-coverage snapshots, reads every other hashed artifact through an inspected/opened/final identity-stable regular-file boundary, exposes and summarizes per-entry snapshot provenance in JSON and HTML, bounds hashes to 20 MB each and 500 MB consumed in aggregate, caps file and region records separately, and represents identity races or aggregate exhaustion explicitly while metadata/semantic accounting continues where safe. Every visited artifact/region is classified as analyzed, indexed, excluded, unresolved, or opaque. Optional coverage JSON uses one exact-byte strict identity-stable 100 MB/100-level/2,000,000-node snapshot, stops at 100,000 file records and 4,096 path characters, binds accepted-byte provenance into settings and the immutable run manifest, reuses internal evidence for repository hashing, keeps external evidence outside repository accounting, and omits repository escapes, parent traversal, malformed coordinates, and duplicate normalized paths with explicit aggregate warnings instead of silently attributing them | `scanner.py`, `manifest.py`, `json_ingestion.py`, `repository_inventory.py`, context/coverage report, `repository-inventory.json` |
| Identify functional and interface failure modes | Baseline functional guidewords plus internal/external interface, data, timing, calculation, logic, state, environment, hardware, resource, detection, and project-specific rules | `scanner.py` candidate rules |
| Consider bad data and event abnormalities | Input/model/serialization rules cover missing, wrong, stale, duplicate, range, precision, compatibility, omission, order, and timing prompts | scanner rules and methodology mapping |
| Identify specific potential causes | Each generated item contains rule-specific cause prompts; reviewers can replace or extend them; an accepted item cannot omit causes by default | analysis schema, validation, browser reviewer |
| Trace local, next-higher, and end effects | Three separate reviewed fields, bounded transitive upstream caller paths, static-versus-runtime evidence labels, cycle/depth/path limits, component/control-scope deduplication with finding-specific ingress, component-diverse priority selection plus repeatable validated finding pins, separately configurable and provenance-recorded HTML/PDF/JSON projection limits with a combined node-budget gate, strict option applicability, separate finding/component coverage, scanner path-inventory completeness, categorical projection status and reason codes exposed in the visual inspector, diagram path/depth omission accounting, timing and uncredited-containment boundaries, hazard linkage, and default completeness gates; exposure is never presented as proven causal propagation | `validation.py`, `architecture.py`, `diagrams.py`, report/diagram CLI, assurance register, reviewer |
| Identify detection methods and compensating provisions | Separate existing prevention and detection controls, evidence, recommended actions, and implemented actions; action-required records cannot omit the action description/owner/date by default. Circuit breakers are first-class static candidates with behavioral-evidence gating, class-scope method correlation, observed-versus-conceptual state labeling, explicit static-evidence review gaps, dedicated failure modes and aggregated diagrams, and finding-specific controlled-clock/concurrency/fault-injection obligations; extraction never credits control effectiveness | model, scanner resilience-control adapter, assurance planner, validation, reviewer, diagrams, exports |
| Identify safety requirements and hazards | Governed requirement/hazard catalogs and component mappings; unknown, unmatched, and orphaned references are reported | `config.py`, `validation.py` |
| Evaluate worst credible consequences | Ground rules and severity rationale are required by default; configured hazard severity may seed but never approve a record | configuration and quality gates |
| Support severity-focused SFMEA and defined organizational scales | Numeric severity, categorical severity, optional numeric S/O/D, and RPN arithmetic are supported | model, configuration, validation |
| Identify corrective actions and assess residual risk | Owner/date, actions taken, verification evidence, post-action ratings/category, and closure gates | model and validation |
| Assess impact of corrective/design changes | Callable, module/class, SFMEA-context, parsed dependency/lockfile, and repository baselines; move/rename continuity; transitive caller impact; explicit revalidation | scanner/store rescan tests |
| Consider dependent/common-mode failures | Project-defined common causes create cross-component items and graph edges; insufficient matches are reported | config, scanner, architecture |
| Maintain the analysis throughout the lifecycle | Stable IDs, generator/schema and verifier name/version provenance, schema migrations, scan history, field-level review history, audit CSV, baseline IDs, malformed-record rejection, standalone run-manifest digest/resolved-input/cross-binding verification, fail-closed mapping-review timestamp credit, strong ETag/`If-Match` stale-write refusal across browser sessions and external edits, byte-stable identical saves for derived assurance/SFTA state, pre-commit disk revision checks, failed-save rollback, loopback Host enforcement, unsaved-edit protection, spreadsheet-safe CSV text, checksum-manifested portable directory/ZIP review packages generated from a privately copied and pre-materialized derived-state snapshot with first-class analysis-reconciled diagnostic, package-provenance, reviewer-view, assurance-register, and hardening-queue projections, explicit capability declarations, cross-version and LF/CRLF semantic compatibility, stable logical ZIP artifact references, atomic publication, bounded streaming hashes and semantic reads, consumption-bounded offline schema-contract loading, iterative analysis node/depth limits, fail-closed core-container/configuration/scalar checks, and sanitized semantic fault containment before hashing or regeneration, flat non-recursive directory verification, independent path- and archive-safe verification, optional detached Ed25519 authenticity, schema-validated workflow status with evidence-backed handoff gates, cross-platform CI, clean distribution smoke tests, dependency monitoring, changelog, and a repeatable release checklist | integrity, validation, store, server, report, signing, schemas, GitHub Actions CI, `sfmea verify-package`, `sfmea status`, `docs/RELEASE.md` |
| Use cross-functional review | Named review team and required decision attribution; missing/unidentified reviewers and approvers are errors, limited role diversity is reported | config, reviewer, validation |
| Document the worksheet and unresolved issues | JSON source record, CSV/Markdown worksheet, self-contained interactive HTML report with transactional generation-time schema-backed exact-binding receipts, locally rendered paginated PDF, inventory, architecture, sequence, traceability, coverage, audit export, deterministic/model summaries, and machine-readable findings. JSON-mode HTML publication stages privately, verifies before atomic replacement, preserves prior output on every rejected phase, rejects symbolic-link/non-regular destinations without following them, and reports explicit publication state without leaking unexpected failures. Custom diagrams use exact-byte identity-stable strict duplicate-free finite JSON ingestion with 5 MB/100-level/250,000-node per-file and 50-file/25 MB aggregate limits; every imported diagram carries accepted-byte SHA-256 provenance under report integrity. Engineering notes use regular-file/link/UTF-8/consumption boundaries and retain portable canonical newlines. HTML embeds a canonical analysis-state declaration plus independent payload and normalized whole-document digests, cross-checks its internal binding, and is independently verifiable with optional exact analysis matching; workflow status reuses the same verifier. Package verification regenerates ten human-facing worksheet, system, audit, guidance, and assurance views in isolation and compares canonical UTF-8 text, rejecting rewritten conclusions even when manifest hashes are recomputed while remaining portable across LF/CRLF platforms. Versioned JSON remains parseable for rejected inputs and distinguishes failed, unchecked, requested, and completed binding checks. Large HTML-only assurance and SFTA workspaces use explicit bounded projections with truthful total counts and links to the complete package registers. Stable bidirectional finding, propagation-node, and assurance-checklist navigation preserves trace context, reports bounded-view omissions, and supplies a copyable state-identified projection recipe | CLI, `diagrams.py`, `html_report.py`, `workflow.py`, `pdf_report.py`, `report.py`, `sfmea report-verify`, `sfmea diagram-verify`, `sfmea verify-package`, and reports |
| Trace authoritative guidance to findings | Versioned NASA/FAA/NIST/IEC/CWE source catalog; explicit applicability profiles; exact section/page locators; canonical source-record and captured-PDF hashes; typed rule mappings; per-finding citations; separate direct/supporting/contextual coverage metrics; rules-without-direct-mapping inventory; mapping-record and locator-summary digests; explicit maintainer-review and independent-approval state; JSON/CSV/package exports; validation; HTML navigation; and a bounded source-to-finding diagram. Mapping digests detect maintained-record drift but do not masquerade as hashes of official prose, and built-ins declare zero independent regulatory approvals. Package verification regenerates the full guidance trace and standalone citation catalog from governed analysis, checks their consistency, and rejects rewritten citations even when manifest hashes are recomputed. Current FAA commercial-space, FAA airworthiness, NASA, security, and legacy contexts cannot leak into a generic scan without profile selection | `guidance.py`, `config.py`, `scanner.py`, `validation.py`, `report.py`, `html_report.py`, `diagrams.py`, `sfmea citations`, `sfmea verify-package` |
| Map every failure mode to an explicit verification obligation | Every active finding receives a stable assurance contract with stimuli, operational context, safe/degraded/recovery expectations, oracles, acceptance criteria, evidence requirements, guarded lifecycle review, package exports, and report navigation. Workflow progress distinguishes accepted/applicable findings, planning readiness, implementation, execution, and independently reviewed verification. The local browser exposes accepted-finding planning with named attribution, stale-write refusal, and read-only evidence/approval states. Pytest scaffolds default to accepted findings and pending implementations, publish atomically, bind exact governed-analysis state, a minimal selected-contract snapshot, and a separately regenerated test-design digest, fail closed on manifest/design drift, and distinguish unrelated analysis changes from materially stale test contracts without treating expected test edits as corruption. Property obligations generate bounded deterministic Hypothesis strategies from retained signatures; contract obligations generate conforming and negative producer/consumer cases tied to candidate contract digests, with unresolved associations retained as failing binding cases. Project adapters fail until they report exact obligation identity, stimulus proof, non-empty evidence, and every oracle and criterion. Verification replays the persisted scope/disposition/limit/implementation selection and emits obligation-level added/removed/changed diffs. Manifest and retirement JSON are consumed through explicit byte bounds; six declared generated-file digests stream under a separate bound without following links; linked/non-regular JSON inputs fail closed; broken retirement links remain visible as invalid present records. The emitted pytest runtime independently enforces a 64 MiB binary-consumption boundary, link/non-file refusal, UTF-8 JSON root/format/integrity/design validation, and complete obligation accounting before collection. Integrity-protected queue ID/owner/purpose metadata gives subsystem/team queues stable organizational identity. Guarded refresh preserves that identity and selection, retains final-path identity, carries the exact verified bounded manifest snapshot forward, revalidates at the publication boundary, requires unchanged manifest identity, publishes through a temporary backup, and restores the previous queue on publication failure. A concurrent independently valid queue replacement is refused without changing the source or leaving staging residue. `sfmea status` recommends refresh only when those preservation checks pass; edited or invalid queues remain inspection/new-destination workflows. A selection that now matches no pending obligations is explicitly reported as a retirement candidate. Guarded archival applies the same mutation-boundary identity comparison; refuses active or edited queues, linked/broken destinations, existing retirement records, and concurrent queue replacement; writes a digest-protected record of the queue, manifest, current analysis binding, and removal diff; then atomically moves the complete directory out of active status paths without deleting evidence. Status discovers conventional paths or verifies a deduplicated set of explicit queues, provides per-queue actions, and calculates current-queue portfolio coverage, assignment overlap with owner/path attribution, duplicate queue IDs, unowned queues, and uncovered accepted obligations while keeping queue organization explicitly outside the mandatory handoff gate | `assurance.py`, `assurance_synthesis.py`, `server.py`, `workflow.py`, `store.py`, `validation.py`, `report.py`, `html_report.py`, `sfmea status`, `sfmea assurance`, `sfmea assurance-scaffold`, `sfmea assurance-scaffold-refresh`, `sfmea assurance-scaffold-archive`, `sfmea assurance-scaffold-verify` |
| Keep generated tests separate from evidence and independently adjudicate results | Test implementations are content-hash bound; an explicitly approved, locked-down Docker/Podman runner records as-run evidence. Existing CI evidence has an idempotent import whose regular UTF-8 JSON manifest and artifact hash/copy streams enforce link-safe consumption-time per-file/aggregate bounds, detect between-pass changes, remove private staging, defer governed mutation until publication, and roll back both analysis and the evidence directory on final recording failure. Managed review reuses bounded link-safe execution-manifest/artifact verification. Verification requires a different reviewer, observed stimulus, every original criterion passing, and a current baseline. Package verification reconciles the exported evidence catalog to the governed baseline and exact execution/artifact inventories, rejecting forged records even when manifest hashes are recomputed | `execution.py`, `validation.py`, `report.py`, HTML evidence workspace, evidence catalog, `sfmea assurance-test-register`, `sfmea assurance-run`, `sfmea assurance-evidence-import`, `sfmea assurance-evidence-review`, `sfmea verify-package` |
| Onboard selected execution evidence through one governed workflow | `sfmea evidence-onboard` validates discovered or explicit coverage, repeated runtime traces, and obligation-bound external execution manifests through the same bounded semantic import paths used by application. Plan mode uses an isolated analysis and temporary managed evidence root, reports prospective changes as validated rather than imported, and leaks no temporary paths. Apply mode requires explicit consent, preserves the source unless in-place publication is requested, projects coverage onto persisted component ranges, copies external artifacts into a managed root, regenerates the run manifest, exactly verifies the assurance queue, and emits a source/result/content-bound receipt with a standalone verifier and public schemas. It never executes repository code, approves evidence, or closes risk | `evidence_onboarding.py`, `scanner.py`, `execution.py`, `runtime.py`, public onboarding receipt/verdict schemas, `sfmea evidence-onboard`, `sfmea evidence-onboard-verify` |
| Use automated discovery without transferring engineering authority | Grounded provider-neutral suggestions use byte/depth/node-bounded strict JSON, exact field/value/count contracts, evidence/citation allowlists, content-addressed provenance, atomic multi-component staging, and rollback-safe review; they remain separately reviewed and materialize only as unreviewed records | `discovery.py`, browser and CLI suggestion workflow |
| Incorporate execution evidence | Idempotent simple and OTLP JSON imports produce baseline-linked observed relations, mapping diagnostics, explicit observed/unavailable/invalid timing state, valid durations, and incompleteness notices. Trace files use exact-byte strict identity-stable duplicate-free finite UTF-8 JSON under 100 MB/100-level/2,000,000-node limits; span, attribute-depth, and label limits are enforced during iterative typed traversal; rejected inputs are side-effect free; runtime spans/edges/provenance/history/summary commit atomically with full analysis rollback on finalization failure | `runtime.py`, sequence export, validation, strict bounded/link/identity-race/rollback/timing tests |
| Analyze machine-readable interface contracts | OpenAPI, Swagger, JSON Schema, and protobuf files become hashed, project-mappable contract components with extracted operations/data types, trace links, and compatibility failure prompts | `scanner.py`, inventory export, extension tests |
| Evaluate automation regression and qualification evidence | A checked-in multi-file synthetic repository and exhaustive 75-case source-aware golden baseline cover framework-style routes/tasks, async behavior, models, control flow, typed receivers, nested-call order, and internal cascades while requiring 1.0 recall/precision, stable repeated-scan digests, and guidance-profile isolation. Ten governed semantic cases add 78 exact deterministic claims across failure modes, causes/local effects/actions, assurance methods, citations, adapters, confidence, and priority, with case/claim and per-field/per-rule mismatch accounting; reviewer-owned system effects, ratings, approval, and runtime proof remain excluded. Four breaker-role records are independently scoped with three negative semantic near-misses, so control precision is false-positive-aware rather than positive-only. Governed campaigns bind retained analyses, independently labeled corpora, and exact evaluation results across repositories; regenerate every result; reject duplicated evidence and masked weak segments; and aggregate finding, call-resolution, control-detection, semantic-output, localization, traceability, citation, and provenance outcomes by rule, field, repository, framework, and domain. A responsive self-contained report surfaces blocker-first gates, searchable bounded rule views, semantic field/rule views, control population counts, artifact identities, governance, and authority limits; staged publication and standalone/exact-result verification detect narrative or payload drift. Completely reconciled campaigns import directly into assurance programs as exact count-backed cohorts. The product supplies the evidence mechanism but cannot authenticate identities, prove sample representativeness, or grant qualification | `benchmarks/python_sfmea_corpus`, `sfmea evaluate -o`, `sfmea qualification-build`, `sfmea qualification-verify`, `sfmea qualification-report`, `sfmea qualification-report-verify`, `sfmea program-init --qualification-*`, qualification/report/evaluation/program/schema/browser tests |
| Record reproducible tool and adapter provenance | An immutable scan manifest binds source/configuration/guidance selection, repository/dependency/contract inventories, environment, command safety, prompt versions, and VCS state; standalone validation recomputes both digests and cross-checks every reproducibility binding, including portable-root redaction, so rehashed false claims remain invalid. The HTML view exposes the verdict. The typed registry publishes versioned capabilities, I/O schemas, trust, isolation, determinism, lifecycle, and run-time health. A hashed execution ledger records each adapter's status, input/output digest, and exact contributed entity IDs; findings retain many-to-one adapter provenance. Package verification regenerates the package-time audit manifest and reviewer instructions, rejects forged decisions behind recomputed internal and outer hashes, and reconciles audit/package timestamps and audit/package/analysis baselines | `integrity.py`, `manifest.py`, `validation.py`, `adapters.py`, `report.py`, run-manifest report view, `adapter-runs.json`, `sfmea verify-package` |
| Apply licensed or organizational guidance without bundling controlled text | Strict local JSON packs contribute an automatically active `org.*` profile, controlled source metadata, revision/scope/access/quote policy, exact locators, typed rule mappings, per-record hashes, and whole-pack provenance. Inputs use exact-byte strict identity-stable duplicate-free finite UTF-8 JSON under 5 MB/100-level/250,000-node limits. ID collisions and compliance claims are rejected | `guidance.py`, `docs/GUIDANCE_PACKS.md`, configuration and scanner tests |
| Exchange findings and component inventory with external tools | SARIF 2.1.0 preserves candidate semantics, stable fingerprints, relative locations, baseline and citation properties; CycloneDX 1.6 records declared dependencies and hashed manifests while explicitly identifying unresolved versions. Package verification regenerates both interchange artifacts with the declared producer version, checks exact projections and shared baseline identity, preserves genuine older packages under newer verifiers, and rejects rewritten content even when manifest hashes are recomputed | `interchange.py`, `report.py`, review package, `sfmea sarif`, `sfmea sbom`, `sfmea verify-package` |
| Compare repository analysis revisions | Canonical run differencing reports new/removed findings, causes/effects/severity/control/trace/citation changes, assumptions, dependency/configuration changes, and invalidated sufficient evidence | `interchange.py`, `sfmea diff`, interchange tests |

## Calibration and qualification evidence added in 0.59

- Golden corpora can carry exhaustive call-resolution labels for named components. Exact overall
  and per-provenance recall/precision now cover raw references, resolved references, resolution
  source, interface confidence, line/order, await state, and lexical context; repeated sites remain
  distinct, and missing or unexpected calls fail evaluation.
- Golden corpora can also carry exact detected-control kind/role labels and governed cohort
  metadata. Results expose per-rule and confidence-label precision plus control recall/precision;
  same-corpus before/after comparison binds named rule changes to distinct author/approver records
  and explicit finding/control recall-regression gates. Supplied independence remains a claim for
  external verification, and an eligible comparison does not apply or approve the change.
- Runtime traces can declare scenario, producer, clock domain, sampling, expected components and
  relationships, dropped spans, and completeness. Import reconciles component scope against mapped
  spans and relationship scope against mapped parent-child spans; validation surfaces incomplete
  declarations.
- Organizational mappings can include a closed, digested independent review record bound to the
  governed source revision, with distinct producer/reviewer identities, authority, dates, and
  rationale. Rejected mappings do not contribute citations, while expired approved reviews are
  audited deterministically against the persisted analysis date and lose effective-approval credit.
- Bounded utilities now produce environment-specific scanner performance evidence, transform a
  reconciled independently reviewed scanner evaluation into a content-addressed, count-backed,
  exact-artifact-bound
  assurance-program cohort with reconciled failure-mode and call-resolution metrics, and calculate
  provider/model/prompt-specific LLM grounding, citation, and claim-weighted unsupported-claim
  metrics from an exact-artifact-bound, subject-bound labeled corpus whose samples are replayed by
  program verification. Program aggregation credits each validation corpus digest once and each
  replayed LLM semantic fingerprint once, so metadata or sample-order repackaging cannot inflate
  the population; declared/credited/duplicate/fingerprinted counts remain visible. Legacy unbound
  corpora are explicitly identified and gated.

These controls make external validation executable and comparable; they do not manufacture the
independent repositories, qualified reviewers, representative scenarios, or organizational
authority that must supply the evidence.

## Boundaries that automation cannot close

- Static analysis cannot completely resolve reflection, monkey-patching, dependency injection, native extensions, runtime service wiring, generated code, or environment-dependent dispatch. Explicit component/interface mappings and runtime evidence remain necessary.
- Source code cannot determine a credible system consequence, hazard severity, independence claim, or risk acceptance without architecture, operational knowledge, and authorized reviewers.
- SFTA correlation does not prove top-down completeness or logical sufficiency. Projects must review and approve their explicit fault-tree logic, analyze non-software contributors, and separately perform any required FTA, STPA, or other hazard analysis.
- Textual test references and line/branch coverage do not prove control effectiveness. Off-nominal tests, fault injection, mutation testing, monitoring evidence, and independent verification must be supplied where required.
- PySFMEA cannot supply a licensed proprietary Action Priority table, domain certification, tool qualification, independence, an identity provider, or a legally controlled approval signature. Optional detached package signatures authenticate freshly verified bytes to a trusted key through strict byte/depth/node-bounded JSON, inspected/opened/final identity-revalidated key/envelope/manifest inputs, and atomic race-refusing publication; they do not provide authorization, revocation, key custody, or approval workflow.
- The governed analysis source is consumed and hashed through a 200 MB identity-stable regular-file boundary, structurally limited to 100 levels and 5,000,000 nodes before migration, and atomically replaced only after bounded serialization and destination revalidation. These controls protect local availability and revision integrity; they do not replace organizational backup, retention, access control, or disaster recovery.
- Standalone CSV/Markdown/JSON/SARIF/CycloneDX/SFTA/visual/diagram/HTML, assurance-register/work-queue, individual-schema, and publication-catalog exports use a shared encoded-artifact boundary with final-link/non-file refusal, synchronized sibling staging, concurrent-destination reconciliation, atomic replacement, prior preservation, exact BOM/newline compatibility, and staging cleanup. Catalog refresh retains its exact pre-validation absent/file state and refuses any later owner or content change. These controls prevent partial or misdirected publication; they do not validate the engineering conclusions or guarantee storage durability beyond the host filesystem.
- Offline schema-bundle and public failure-catalog verification use strict governed-JSON ingestion with byte, depth, and node limits; regular non-link enforcement; inspected/opened/final identity reconciliation; exact UTF-8; and duplicate-key/non-finite rejection before canonical hashing. These checks remove ambiguous or unstable verifier inputs but do not authenticate a contract publisher or approve its use.
- Assurance work queues, scaffold/retirement manifests, and imported/recorded execution manifests use the same strict identity-stable JSON primitive with contract-scaled byte/depth/node ceilings and numeric-overflow rejection before lifecycle or evidence decisions. The generated pytest scaffold embeds an independent equivalent parser/structure guard. These controls preserve deterministic interpretation and availability; they do not establish evidence authenticity, test adequacy, reviewer independence, or execution authorization.
- Occurrence is not inferred from code metrics. If used, its project meaning and evidence must be defined by the responsible organization.
- A valid assurance-program verdict establishes configured digest, trusted-evidence, reference,
  metric, timing, circuit-breaker, and program-role consistency only. It does not discover
  undeclared services, prove cross-service causality or
  schedulability, authenticate a named reviewer, establish corpus representativeness, or replace
  enterprise authorization and records controls.

## Audit sources

1. [NASA Software Engineering Handbook — Software Failure Modes and Effects Analysis](https://swehb.nasa.gov/spaces/SWEHBVD/pages/102695706/8.05+-+SW+Failure+Modes+and+Effects+Analysis)
2. [FAA Guide to Reusable Launch and Reentry Vehicle Software and Computing System Safety, Appendix B](https://www.faa.gov/sites/faa.gov/files/regulations_policies/faa_regulations/commercial_space/Guide-Software-Comp-Sys-Safety-RLV-Reentry.pdf)
3. [NASA-STD-8739.8B Software Assurance and Software Safety Standard](https://standards.nasa.gov/standard/nasa/nasa-std-87398)
4. [NASA Software Safety Guidebook, NASA-GB-8719.13](https://standards.nasa.gov/sites/default/files/standards/NASA/Baseline/0/nasa-gb-871913.pdf)
5. [FAA AC 450.141-1A, Computing System Safety](https://www.faa.gov/regulations_policies/advisory_circulars/index.cfm/go/document.information/documentNumber/450.141-1A)
6. [FAA AC 20-115D, Airborne Software Development Assurance](https://www.faa.gov/airports/resources/advisory_circulars/index.cfm/go/document.information/documentNumber/20-115D)
7. [IEC 60812:2018 publication page](https://webstore.iec.ch/en/publication/26359) — referenced but not reproduced.
